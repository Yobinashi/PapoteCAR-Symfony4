{% extends 'base.html.twig' %}

{% block body %}

    <h1>{{ run.departure }} -> {{ run.arrival }}</h1>

    Pasengers:
    <ul>
    {% for passenger in run.passengers %}
       <li> {{ passenger.firstname }}</li>
    {% else %}
    {% endfor %}

    </ul>
{% endblock %}